### Navy quick launch
# show a list of commands and keybinds

local navy="mob    [mobile app]
cms    [CMS]"

local cmd=$(echo $navy | eval "fzf ${FZF_DEFAULT_OPTS} --header='[navy:show]'" | awk '{print $1}')

if [[ -n $cmd ]]; then
 eval ${cmd}
fi
